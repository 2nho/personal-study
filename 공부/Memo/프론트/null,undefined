둘다 메모리에 주소를 가르키고 있지 않기 때문에 가비지 컬렉터가 실행됨.

다만 null 과 undefined는 글자 길이만큼 4바이트 차이가 나며
null은 사용자가 직접 설정한 값
undefined는 자바스크립트 엔진이 설정한 값으로 볼 수 있다(undefined로 직접 변수 초기화를 하지 않은 이상!)
- let a = undefined로 초기화하면 추후 변수를 초기화안해서 생긴 undefined인지 아님 undefined로 의도적으로 설정한 것인지
헷갈리기 때문에 null로 초기화하는겂이 옳은 방법
