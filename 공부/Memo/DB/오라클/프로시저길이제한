json파싱중 문자열이 3만자가 넘어가면 오류발생
줄바꿈으로 나눠주면 오류가 안발생
폐쇄망에서 발생하는거라 재확인필요




EXCEPTION WHEN OTHERS THEN	
		ROLLBACK;	
        p_nResult := -204;
        p_sMsg := SQLERRM;
 SQLERRM를 이용해서 오류메세지 출력 후 확인 





ORA-00001 무결성 제약 조건

ON 절에 사용되는 컬럼은 업데이트 불가 

ORA-30926 원본테이블의 고정 행 집합을 가져올 수 없습니다.
 
